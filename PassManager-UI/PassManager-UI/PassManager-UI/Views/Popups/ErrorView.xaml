<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             x:Class="PassManager.Views.Popups.ErrorView"
             CloseWhenBackgroundIsClicked="False">
    <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Margin="20" Padding="20" BackgroundColor="{StaticResource FrameTopColor}">
        <Label FontSize="Medium" Text="{Binding PageTitle}" TextColor="#e63946"/>
        <StackLayout Orientation="Horizontal" Padding="20">
            <Button IsVisible="{Binding CanQuit}" Text="Quit" Command="{Binding Quit}" Style="{StaticResource Key=PopupBtn}"/>
            <Button Text="Ok" Command="{Binding ClosePopup}" Style="{StaticResource Key=PopupBtn}"/>
        </StackLayout>
    </StackLayout>
</pages:PopupPage>