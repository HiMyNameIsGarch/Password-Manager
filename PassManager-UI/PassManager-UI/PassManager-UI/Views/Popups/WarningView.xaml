<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             x:Class="PassManager.Views.Popups.WarningView">
    <StackLayout Orientation="Vertical" VerticalOptions="Center" HorizontalOptions="Center" 
                 BackgroundColor="{DynamicResource PrussianBlueColor}" Padding="30" Margin="30">
        <Label Text="{Binding PageTitle}" TextColor="{StaticResource OrangeColor}" FontSize="Medium" VerticalTextAlignment="Center"/>
        <Button Text="Ok" Command="{Binding ClosePopup}" Style="{StaticResource Key=PopupBtn}"/>
    </StackLayout>
</pages:PopupPage>