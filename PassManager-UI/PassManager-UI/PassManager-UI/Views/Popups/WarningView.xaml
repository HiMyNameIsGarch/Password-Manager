<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             x:Class="PassManager.Views.Popups.WarningView">
    <StackLayout Style="{DynamicResource PopupFrame}">
        <Label Text="{Binding PageTitle}" TextColor="{DynamicResource OrangeColor}" FontSize="{OnPlatform Android=20,UWP=22}" VerticalTextAlignment="Center" Margin="10"/>
        <Button Text="Ok" Command="{Binding ClosePopup}" Style="{DynamicResource PopupBtn}"/>
    </StackLayout>
</pages:PopupPage>