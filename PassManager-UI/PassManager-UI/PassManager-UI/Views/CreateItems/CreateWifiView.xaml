<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PassManager.Views.CreateItems.CreateWifiView" Title="{Binding PageTitle}">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior Command="{Binding GoBack}" IsEnabled="True"/>
    </Shell.BackButtonBehavior>
    <Shell.TitleView>
        <Label Text="{Binding PageTitle}" FontSize="20" VerticalTextAlignment="Center"/>
    </Shell.TitleView>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Here you can create a wifi"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" TextColor="Green" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>