<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PassManager.Views.FlyoutItems.NoteView" Title="{Binding PageTitle}"
             ControlTemplate="{StaticResource ListViewItemTemplate}">
    <Shell.TitleView>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Style="{DynamicResource NavTitleLabel}"/>
            <Button Grid.Column="1" Grid.ColumnSpan="2" Style="{DynamicResource SearchNavBtn}"/>
        </Grid>
    </Shell.TitleView>
</ContentPage>